Algorithm Selection_sort(A[0..n-1])
// Сортування масиву методом вибору
// Вхідні дані: масив A[0..n-1], що впорядковується
// Вихідні дані: масив A[0..n-1], відсортований у неспадному порядку

for i ← 0 to n-2 do       // зовнішній цикл
{
    min ← i
    for j ← i+1 to n-1 do // внутрішній цикл
        if A[j] < A[min] then
            min ← j
    
    // обмін A[i] та A[min]
    B ← A[i]
    A[i] ← A[min]
    A[min] ← B
}

